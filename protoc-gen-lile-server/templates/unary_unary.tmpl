package server

import (
	"errors"

	context "golang.org/x/net/context"

  "{{ .GoPackage }}"
)

func (s Server) {{ .Name }}(ctx context.Context, r *{{ .InType }}) (*{{ .OutType }}, error) {
	return nil, errors.New("not yet implemented")
}
